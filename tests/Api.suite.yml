actor: ApiTester
#suite_namespace: App\Tests\Api
modules:
    enabled:
        - Asserts
        - Symfony:
              app_path: 'src'
              environment: 'test'
        - REST:
              depends: Symfony
        - Doctrine:
              depends: Symfony
              cleanup: true
