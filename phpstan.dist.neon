includes:
    - vendor/phpstan/phpstan-strict-rules/rules.neon

parameters:
    level: max
    paths:
        - src/
    treatPhpDocTypesAsCertain: false
    ignoreErrors:
        # Traits are analyzed when used by entities; unused trait warnings are expected in scaffolds
        - identifier: trait.unused
